// ============================================
// DATABASE CONFIG
// Centraliza la configuración de la BD
// ============================================
export const databaseConfig = {
  url: process.env.DATABASE_URL || '',
};
